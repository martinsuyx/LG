import { test } from '@playwright/test';

// Skeleton e2e specification for the AI Intake flow.
// Replace todos with concrete interactions once the page implementation exists.

test.describe('AI Intake Page', () => {
  test.todo('uploads multiple images and receives AI parsing status updates');
  test.todo('edits low-confidence fields before submission');
  test.todo('submits confirmed fields and navigates to order detail');
  test.todo('handles AI failure path by switching to manual intake');
  test.todo('records required analytics or audit events during the flow');
});
