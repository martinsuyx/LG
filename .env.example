# ============================

# 环境变量模板 (.env.example)

# Joincom Admin Demo (P0–P3)

# ============================

# ---------- 基础配置 ----------

NODE_ENV=development
PORT=8080
HOST=0.0.0.0
TZ=Asia/Shanghai

# ---------- 数据库 ----------

# PostgreSQL 本地数据库配置 (docker-compose)

DATABASE_URL=postgresql://demo:demo@localhost:5432/demo
DB_USER=demo
DB_PASSWORD=demo
DB_NAME=demo
DB_HOST=localhost
DB_PORT=5432

# ---------- 对象存储 ----------

# 本地使用 MinIO 模拟 OSS (docker-compose)

S3_ENDPOINT=[http://localhost:9000](http://localhost:9000)
S3_ACCESS_KEY=demo
S3_SECRET_KEY=demodemo
S3_BUCKET=joincom-demo
S3_REGION=cn-guangzhou
S3_CDN_DOMAIN=[http://localhost:9000](http://localhost:9000)

# ---------- JWT / Auth ----------

JWT_SECRET=dev_jwt_secret_for_demo_only
JWT_ISSUER=joincom-admin
JWT_AUDIENCE=joincom-clients
ACCESS_TOKEN_TTL_MINUTES=30
REFRESH_TOKEN_TTL_DAYS=7

# ---------- 第三方回调 (模拟) ----------

# 用于 KYC / 支付 / 风控 Provider 模拟回调

CALLBACK_BASE_URL=[http://localhost:8080/api/v1](http://localhost:8080/api/v1)
KYC_PROVIDER_URL=[http://localhost:8080/mock/kyc](http://localhost:8080/mock/kyc)
RISK_PROVIDER_URL=[http://localhost:8080/mock/risk](http://localhost:8080/mock/risk)
PAYOUT_PROVIDER_URL=[http://localhost:8080/mock/payout](http://localhost:8080/mock/payout)

# ---------- Feature Flags / 灰度 ----------

FEATURE_WALLET_V2=false
FEATURE_NEW_KYC=true

# ---------- 系统设置 (I18N) ----------

DEFAULT_TIMEZONE=Asia/Shanghai
DEFAULT_CURRENCY=CNY
MONEY_DECIMAL_SCALE=2
PERCENT_DECIMAL_SCALE=1

# ---------- 邮件 / 通知 (Mock) ----------

SMTP_HOST=localhost
SMTP_PORT=2525
SMTP_USER=demo
SMTP_PASS=demo
NOTIFY_CHANNELS=email,feishu

# ---------- 前端配置 (Vite) ----------

VITE_API_BASE=[http://localhost:4010](http://localhost:4010)
VITE_ENV=development

# ---------- 审计与日志 ----------

AUDIT_RETENTION_DAYS=365
AUDIT_WORM_STORAGE=false

# ---------- 调试与开发 ----------

LOG_LEVEL=debug
ENABLE_SEED_DATA=true
ENABLE_MOCK_PROVIDER=true

# End of .env.example
